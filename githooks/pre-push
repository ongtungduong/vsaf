#!/usr/bin/env bash
set -euo pipefail

echo "==> Pre-push: Running verification checks..."

make verify || { echo "BLOCKED: 'make verify' failed."; exit 1; }
make scan   || { echo "BLOCKED: 'make scan' failed."; exit 1; }

echo "==> Pre-push checks passed."
